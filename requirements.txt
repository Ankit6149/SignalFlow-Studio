# Narrow optional Python utility dependencies.
# The production application and inference APIs live in frontend/.
# FFmpeg is required on PATH for terminal recording to produce MP4/GIF.
Pillow
Pygments
reportlab
markdown2
pytest
