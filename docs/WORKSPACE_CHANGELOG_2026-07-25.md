# Workspace consolidation — 2026-07-25

- Replaced the layered Studio override file with `app-workspace.css`.
- Scoped all application visuals under `.app-shell`.
- Preserved landing-page styles and motion outside the application.
- Removed inherited card lifting, shimmer, pulse, dark selection, and fixed command-bar behaviour.
- Joined Source, Destinations, and the command footer into one continuous workspace surface.
- Standardized centered content widths and responsive viewport gutters.
- Forced light browser controls for consistent form rendering across laptops.
