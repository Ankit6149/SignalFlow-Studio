import PlanWorkspace from "../../components/PlanWorkspace";

export const metadata = {
  title: "Plan · SignalFlow Studio",
  description: "Review worthwhile content opportunities and choose the narrative direction before generation.",
};

export default function PlanPage() {
  return <PlanWorkspace />;
}
