import VoiceWorkspace from "../../components/VoiceWorkspace";

export const metadata = {
  title: "Voice · SignalFlow Studio",
  description: "Define explicit identity, desired perception, voice, boundaries and platform expression for SignalFlow generation.",
};

export default function VoicePage() {
  return <VoiceWorkspace />;
}
