name: Apply campaign freshness integration

on:
  pull_request:
    branches:
      - master
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  integrate:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Detect integration work
        id: integration
        shell: bash
        run: |
          if [ -f scripts/apply-campaign-freshness.mjs ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Node
        if: steps.integration.outputs.run == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Correct temporary codemod escaping
        if: steps.integration.outputs.run == 'true'
        run: node scripts/fix-campaign-freshness-codemod.mjs

      - name: Apply source transformation
        id: transform
        if: steps.integration.outputs.run == 'true'
        continue-on-error: true
        shell: bash
        run: node scripts/apply-campaign-freshness.mjs > integration-diagnostics.txt 2>&1

      - name: Report transformation failure
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'failure'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          {
            echo "### Campaign freshness integration diagnostic"
            echo
            echo '```text'
            cat integration-diagnostics.txt
            echo '```'
          } > integration-comment.md
          gh pr comment "${{ github.event.pull_request.number }}" --body-file integration-comment.md
          exit 1

      - name: Install frontend dependencies
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'success'
        working-directory: frontend
        run: npm ci

      - name: Run frontend regression tests
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'success'
        working-directory: frontend
        run: npm test

      - name: Audit frontend production dependencies
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'success'
        working-directory: frontend
        run: npm audit --omit=dev --audit-level=high

      - name: Build frontend
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'success'
        working-directory: frontend
        run: npm run build

      - name: Run MCP tests
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'success'
        working-directory: mcp
        run: npm test

      - name: Remove temporary integration files
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'success'
        run: rm .github/workflows/apply-campaign-freshness.yml scripts/apply-campaign-freshness.mjs scripts/fix-campaign-freshness-codemod.mjs

      - name: Commit verified integration
        if: steps.integration.outputs.run == 'true' && steps.transform.outcome == 'success'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: track campaign source freshness"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
