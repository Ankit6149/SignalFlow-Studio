# Temporary GP2 Worker Trigger

This branch exists only to emit a non-merged pull-request webhook so production can run one pending GP2 continuation job. It is not intended to merge and must not be treated as product work.
